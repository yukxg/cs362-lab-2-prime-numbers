#ifndef LLIST_H
#define LLIST_H

#include<iostream>
using namespace std;

class LList {
	public:
		LList();
		LList(int);
		~LList();

		void printList();
		void printList(int);

		//void removeMult(int);
	private:

};
#endif
